Frame: 360 height
Grid: 150 height

Temple:
270 from top to grid ('horizon')
480 total

Sewer:
About 370 from top to grid ('horizon')
525 total height

Camera serves as:
-Letting us know the scaling factor between the stage and the screen
-Telling us how much of the stage is off screen, horizontally, with camera.left
-Clipping area of the stage, which becomes its own surface


static stage elements, animated stage elements
things that are flat on the floor
grid lines
sort by row, then anchor point, a variable that determines where the object is sticking out of the ground
foreground effects, like fog that's closer than the closest row